# For local dev, Vite proxies /api to http://localhost:8000 (see vite.config.ts) — leave this empty.
# For Vercel (production), set this to your deployed backend's HTTPS URL:
#   VITE_API_BASE=https://your-backend.onrender.com
VITE_API_BASE=
